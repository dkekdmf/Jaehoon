#include <stdio.h>
#pragma warning(disable:4996)

int main(void) {

	char N[100];
	int max = -10000;
	int count = 0;
	scanf("%s", N);

	for (int i = 0; strlen(N); i++) {
		if (N[i] )
			
	}
	

	return 0;
}