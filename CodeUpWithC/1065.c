#include <stdio.h>
#pragma warning(disable:4996)

int main_1065(void)
{
	int a, b, c;
	scanf("%d %d %d", &a, &b, &c);

	if (a % 2 == 0) {
		printf("%d\n", a);
	}
	if (b % 2 == 0) {

		printf("%d\n", b);
	}
	if (c % 2 == 0) {

		printf("%d\n", c);
	}

	return 0;
}